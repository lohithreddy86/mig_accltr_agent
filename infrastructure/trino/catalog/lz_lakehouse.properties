# =============================================================================
# Catalog: lz_lakehouse
# Purpose: TARGET catalog for migrated writes (INSERT/UPDATE/DELETE/MERGE/CTAS).
#          Backed by Iceberg tables on MinIO, metadata in Hive Metastore.
# Schema:  lm_target_schema  (created post-startup; see infrastructure/init/)
# =============================================================================

connector.name=iceberg

# Hive Metastore (Thrift)
iceberg.catalog.type=hive_metastore
hive.metastore.uri=thrift://hive-metastore:9083

# File format defaults
iceberg.file-format=PARQUET
iceberg.compression-codec=ZSTD

# Native S3 file system (Trino 4xx)
fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.region=us-east-1
s3.path-style-access=true
s3.aws-access-key=minio
s3.aws-secret-key=minio123

# Iceberg behaviour
iceberg.register-table-procedure.enabled=true
iceberg.unique-table-location=true
